# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/contacthub?retryWrites=true&w=majority

# Port (optional — defaults to 5000)
PORT=5000

# Frontend URL for CORS (set this after deploying frontend)
FRONTEND_URL=http://localhost:5173
